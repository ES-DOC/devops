# ###############################################################
# PROLOGUE
# ###############################################################

# Ensure that environment supports alias expansion.
shopt -s expand_aliases

# Set home.
export INSTALLER_HOME="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
export INSTALLER_GLOBAL="$( cd "$( dirname "${INSTALLER_HOME[0]}" )" && pwd )"/global

# Ensure shell scripts are executable.
chmod a+x $INSTALLER_HOME/sh/*.sh
chmod a+x $INSTALLER_GLOBAL/sh/*.sh

# ###############################################################
# ALIASES
# ###############################################################

alias pyessv-installer-step-01=$INSTALLER_HOME/sh/step_01.sh
alias pyessv-installer-step-02=$INSTALLER_HOME/sh/step_02.sh
alias pyessv-installer-step-03=$INSTALLER_HOME/sh/step_03.sh
alias pyessv-installer-step-04=$INSTALLER_HOME/sh/step_04.sh
alias pyessv-installer-step-05=$INSTALLER_HOME/sh/step_05.sh
